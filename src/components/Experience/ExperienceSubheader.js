import styled from 'styled-components'
import P from '../../elements/P'
import * as colors from '../../theme/colors'

const ExperienceSubHeader = styled(P)`
  font-weight: 400;
  font-family: 'Roboto';
  font-size: 18px;
  margin-bottom: 40px;
  color: ${colors.darkSubheader};
`

export default ExperienceSubHeader
