import styled from 'styled-components'

const ContactFormNameContainer = styled.div`
  display: flex;
  flex-flow: row wrap;
  justify-content: space-between;
  padding: 0;
`

export default ContactFormNameContainer
