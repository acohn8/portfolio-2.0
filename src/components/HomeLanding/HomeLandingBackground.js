import styled from 'styled-components'
import * as colors from '../../theme/colors'

const HomeLandingBackground = styled.div`
  height: 500px;
  width: 100%;
  background-color: ${colors.navBar};
`

export default HomeLandingBackground
